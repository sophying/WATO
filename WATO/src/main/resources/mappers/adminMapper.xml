<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin">

	<!-- admin 페이지에서 강사 승인 -->
	<select id="adminmanage" resultType="com.king.myapp.domain.ApprovalVO">
		SELECT
			app_Profile, app_Resume, app_Gender, 
			app_Phone1, app_Phone2, app_Phone3, 
			app_Email,
			app_Addr1, app_Addr2, app_Addr3
		FROM Approval
	</select>
	
	<!-- 비밀번호 찾기 -->
	<select id="findidpwd" resultType="com.king.myapp.domain.TeachVO"> 
		SELECT * FROM Teach_info WHERE User_ID = #{User_Id} and Teach_email = #{Teach_Email}
	</select>
	
	<!-- 임시비밀번호발급(비밀번호 변경됨) -->
	<update id="changepwd" parameterType="com.king.myapp.domain.TeachVO"> 
		UPDATE teach_info
		set User_Pwd = #{User_Pwd} where Teach_email = #{Teach_Email}
	</update>
</mapper>