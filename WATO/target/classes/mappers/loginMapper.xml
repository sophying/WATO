<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="join"> 
   <insert id="insert">
  <!--  
	M_NO NUMBER,
	M_USERNAME varchar2(30),
	m_user_id varchar2(30),
	m_user_pwd varchar2(50),
	m_regdate date default sysdate,
	primary key(m_user_id)
);
 
  -->
   		
   		insert into studymember (m_no, m_username, m_user_id, m_user_pwd, m_regdate)
   		values ((select nvl(max(m_no),0)+1 from studymember), #{m_username}, #{m_user_id}, #{m_user_pwd}, SYSDATE)
   
   
   </insert>
   
   
   
   
   <select id="login" resultType="com.king.myapp.domain.MemberVO">
   	select m_user_id 
   	from studymember
   	where m_user_id= #{m_user_id} and m_user_pwd=#{m_user_pwd}
   </select>
</mapper>
